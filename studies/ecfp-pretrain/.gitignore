# Regenerated by the pipeline, too large to track.
data/
folds/
predictions/
logs/
results/*/figures/*
!results/*/figures/tukey_r2.png
!results/*/figures/ptgin_selection.png
results/.ptgin_done
# The 10 x endpoint x fold validation sweep: 3,750 rows on ExpansionRx and
# 1,500 on Biogen. The aggregates the report reads -- ptgin_choice.csv and
# tables/selection_summary.csv -- stay tracked.
results/*/ptgin_selection.csv
results/*/predictions_all.parquet

# The report is built here and published from docs/reports/ at the top level.
results/report.html

# Reference material that is not ours to redistribute.
*.pdf
qmugs.csv.gz

# Third-party pre-trained weights. PTGIN_HOME points at the authors' checkout;
# nothing is vendored here.
*.pt
*.pth

__pycache__/
*.pyc
.DS_Store
